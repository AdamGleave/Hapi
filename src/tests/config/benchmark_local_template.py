#DATASET_ROOT = "/path/to/flownetworks"
# Path flow networks for full and incremental offline tests are stored in

#TRACE_ROOT = "/path/to/trace"
# Path trace(s) in the Google format are stored in

#FIRMAMENT_ROOT = "/path/to/firmament"
# Defaults to being in the directory PROJECT_ROOT is in, with name "firmament"

#MAKE_FLAGS=["-j", "NUMBER_OF_CORES"]
# Number of cores to use when building applications.